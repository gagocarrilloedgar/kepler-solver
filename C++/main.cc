
// Headers
#include "headers.h"

int main()
{

    cout << "random= " << delta << endl;
    vector<double> e = linspace(0.8, 1.0, N); // Orbit's eccentricity

    for (auto x : e)
        cout << x << endl;
}
